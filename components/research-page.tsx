"use client";
import { useState } from "react";
import { directions } from "@/lib/demo-data";
import { AppShell } from "./app-shell";

export function ResearchPage(){const [direction,setDirection]=useState(directions[0]);const [editing,setEditing]=useState(false);return <AppShell section="My Research"><div className="content"><div className="page-head"><div><h1>My Research</h1><p className="subtle">研究方向不是文件夹，而是 Agent 分析文献时必须遵守的上下文与边界。</p></div><button className="btn" onClick={()=>setEditing(!editing)}>{editing?"完成编辑":"编辑方向"}</button></div><section className="research-hero"><span className="eyebrow">Research Direction · Active</span>{editing?<><input className="field" style={{margin:"15px 0 10px",fontSize:20}} value={direction.title} onChange={e=>setDirection({...direction,title:e.target.value})}/><textarea className="field" value={direction.description} onChange={e=>setDirection({...direction,description:e.target.value})}/></>:<><h1 style={{maxWidth:950,marginTop:15}}>{direction.title}</h1><p className="subtle" style={{maxWidth:780}}>{direction.description}</p></>}<div className="tag-row">{direction.keywords.map(k=><span className="tag" key={k}>{k}</span>)}</div></section><div className="research-columns section"><ResearchList title="Related Papers" items={direction.relatedPapers}/><ResearchList title="Related Methods" items={direction.relatedMethods}/><ResearchList title="Related Problems" items={direction.relatedProblems}/><ResearchList title="Related Concepts" items={direction.relatedConcepts}/></div><section className="section grid cols-2"><div className="card"><h2>My Notes</h2>{editing?<textarea className="field" value={direction.notes} onChange={e=>setDirection({...direction,notes:e.target.value})}/>:<p className="subtle">{direction.notes}</p>}</div><div className="card gap-box"><span className="eyebrow">Potential Research Gaps · Placeholder</span><h2 style={{marginTop:12}}>等待证据积累</h2><p className="subtle">下一阶段将通过已读文献、个人笔记和可信图关系识别候选缺口。当前不自动生成科研建议，避免脱离证据。</p></div></section></div></AppShell>}
function ResearchList({title,items}:{title:string;items:string[]}){return <div className="card"><h2>{title}</h2><ul className="research-list">{items.map(i=><li key={i}>{i}</li>)}</ul></div>}
